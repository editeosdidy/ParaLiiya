<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Para mi amore Liiya ❤️</title>
    <style>
        /* --- DISEÑO GENERAL (CSS) --- */
        * {
            box-sizing: border-box;
            margin: 0;
            padding: 0;
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
        }

        body {
            background: linear-gradient(135deg, #ff9a9e 0%, #fecfef 50%, #a1c4fd 100%);
            height: 100vh;
            display: flex;
            justify-content: center;
            align-items: center;
            overflow: hidden;
            position: relative;
        }

        /* Corazones Flotantes */
        .heart-particle {
            position: absolute;
            color: rgba(255, 105, 180, 0.6);
            font-size: 20px;
            animation: floatUp 6s linear infinite;
            bottom: -50px;
            z-index: 1;
            pointer-events: none;
        }

        @keyframes floatUp {
            0% { transform: translateY(0) rotate(0deg); opacity: 1; }
            100% { transform: translateY(-105vh) rotate(360deg); opacity: 0; }
        }

        /* Contenedor de Pantallas */
        .screen {
            background: rgba(255, 255, 255, 0.85);
            backdrop-filter: blur(10px);
            padding: 30px;
            border-radius: 20px;
            box-shadow: 0 10px 30px rgba(0, 0, 0, 0.1);
            text-align: center;
            max-width: 450px;
            width: 90%;
            z-index: 10;
            display: none;
            transform: scale(0.8);
            opacity: 0;
            transition: all 0.5s ease;
        }

        .screen.active {
            display: block;
            transform: scale(1);
            opacity: 1;
        }

        /* Títulos y Texto */
        h1 { color: #d63384; margin-bottom: 20px; font-size: 1.8rem; }
        h2 { color: #ff4757; margin-bottom: 20px; }
        p { color: #4b4b4b; line-height: 1.6; font-size: 1.1rem; }

        /* Botones Generales */
        button {
            background: #ff4757;
            color: white;
            border: none;
            padding: 12px 25px;
            margin: 10px;
            font-size: 1rem;
            font-weight: bold;
            border-radius: 50px;
            cursor: pointer;
            box-shadow: 0 4px 15px rgba(255, 71, 87, 0.3);
            position: relative;
            overflow: hidden;
            transition: transform 0.2s, box-shadow 0.2s;
        }

        button:hover {
            transform: translateY(-2px);
            box-shadow: 0 6px 20px rgba(255, 71, 87, 0.5);
        }

        /* Efecto de Brillo en Botones */
        button::after {
            content: '';
            position: absolute;
            top: -50%; left: -60%;
            width: 30%; height: 200%;
            background: rgba(255, 255, 255, 0.4);
            transform: rotate(30deg);
            transition: none;
        }

        button:hover::after {
            left: 120%;
            transition: all 0.6s ease-in-out;
        }

        button.btn-back {
            background: #6c757d;
            box-shadow: 0 4px 15px rgba(108, 117, 125, 0.3);
            margin-top: 20px;
        }
        button.btn-back:hover { box-shadow: 0 6px 20px rgba(108, 117, 125, 0.5); }

        /* PANTALLA 1: Regalo */
        #screen1 h1 { font-size: 2.2rem; text-shadow: 1px 1px 2px rgba(0,0,0,0.1); }
        .gift-box {
            font-size: 100px;
            cursor: pointer;
            display: inline-block;
            animation: bounce 2s infinite;
            user-select: none;
        }

        @keyframes bounce {
            0%, 20%, 50%, 80%, 100% { transform: translateY(0); }
            40% { transform: translateY(-20px); }
            60% { transform: translateY(-10px); }
        }

        /* PANTALLA 2: Menú */
        .menu-grid {
            display: grid;
            grid-template-columns: 1fr 1fr;
            gap: 15px;
        }
        .menu-grid button { margin: 0; padding: 20px; font-size: 1.1rem; }

        /* SECCIÓN: Carta */
        .letter-box {
            background: #fffdf0;
            border: 1px solid #f3e9b7;
            padding: 25px;
            border-radius: 10px;
            box-shadow: inset 0 0 20px rgba(0,0,0,0.05), 0 5px 15px rgba(0,0,0,0.05);
            text-align: left;
            transform: scaleY(0);
            transform-origin: top;
            animation: unfold 1s forwards ease-in-out;
        }

        @keyframes unfold {
            to { transform: scaleY(1); }
        }

        /* SECCIÓN: ¿Me quieres? */
        .quiz-container { position: relative; height: 120px; margin-top: 20px; }
        #btn-no { position: absolute; }

        /* SECCIÓN: Foto */
        .photo-frame {
            border: 12px solid white;
            border-bottom: 35px solid white;
            border-radius: 4px;
            box-shadow: 0 8px 20px rgba(0,0,0,0.15);
            display: inline-block;
            margin-bottom: 15px;
            position: relative;
            overflow: hidden;
        }
        .photo-frame img {
            max-width: 100%;
            height: auto;
            display: block;
            max-height: 250px;
        }
        /* Efecto brillo en foto */
        .photo-frame::after {
            content: '';
            position: absolute;
            top: 0; left: -150%;
            width: 50%; height: 100%;
            background: linear-gradient(to right, rgba(255,255,255,0) 0%, rgba(255,255,255,0.3) 50%, rgba(255,255,255,0) 100%);
            transform: skewX(-25deg);
            animation: photoShine 4s infinite linear;
        }
        @keyframes photoShine {
            0% { left: -150%; }
            30%, 100% { left: 150%; }
        }

        /* SECCIÓN: Canciones */
        .song-list { list-style: none; text-align: left; }
        .song-item {
            background: rgba(255, 71, 87, 0.1);
            padding: 12px;
            margin: 8px 0;
            border-radius: 10px;
            cursor: pointer;
            font-weight: 600;
            color: #ff4757;
            transition: background 0.2s;
        }
        .song-item:hover { background: rgba(255, 71, 87, 0.2); }
        #song-message { margin-top: 15px; font-weight: bold; color: #d63384; min-height: 24px; }
    </style>
</head>
<body>

    <!-- --- PANTALLAS (HTML) --- -->

    <!-- Pantalla 1: El Regalo -->
    <div id="screen1" class="screen active">
        <h1>Para mi amore Liiya ❤️</h1>
        <div class="gift-box" onclick="changeScreen('screen2')">🎁</div>
        <p style="margin-top: 15px; font-size: 0.9rem; color: #888;">Haz clic en el regalo para abrir</p>
    </div>

    <!-- Pantalla 2: El Menú Principal -->
    <div id="screen2" class="screen">
        <h2>¿Qué deseas ver hoy?</h2>
        <div class="menu-grid">
            <button onclick="changeScreen('screen-letter')">💌 Carta</button>
            <button onclick="changeScreen('screen-quiz')">💖 ¿Me quieres?</button>
            <button onclick="changeScreen('screen-photo')">🖼️ Nuestra foto</button>
            <button onclick="changeScreen('screen-songs')">🎵 Canciones</button>
        </div>
    </div>

    <!-- Sección: Carta -->
    <div id="screen-letter" class="screen">
        <h2>Para Ti</h2>
        <div class="letter-box">
            <p>Mi querida Liiya:</p><br>
            <p>Este pequeño regalo está hecho con mucho cariño. Espero que cada vez que lo abras recuerdes lo especial que eres para mí.</p><br>
            <p>Gracias por llenar mis días de felicidad. ❤️</p>
        </div>
        <button class="btn-back" onclick="changeScreen('screen2')">Volver</button>
    </div>

    <!-- Sección: ¿Me quieres? -->
    <div id="screen-quiz" class="screen">
        <h2 id="quiz-title">¿Me quieres? 🥺</h2>
        <div class="quiz-container" id="quiz-actions">
            <button id="btn-yes" onclick="selectYes()">❤️ Sí</button>
            <button id="btn-no" onmouseover="moveNoButton()" onclick="moveNoButton()">💔 No</button>
        </div>
        <button class="btn-back" onclick="resetQuiz(); changeScreen('screen2')">Volver</button>
    </div>

    <!-- Sección: Nuestra Foto -->
    <div id="screen-photo" class="screen">
        <h2>Nosotros ✨</h2>
        <div class="photo-frame">
            <!-- Reemplaza esta URL por la de tu foto real -->
            <img src="https://picsum.photos" alt="Nuestra Foto">
        </div>
        <p>¡Cada momento a tu lado es único!</p>
        <button class="btn-back" onclick="changeScreen('screen2')">Volver</button>
    </div>

    <!-- Sección: Canciones -->
    <div id="screen-songs" class="screen">
        <h2>Nuestras Canciones 🎶</h2>
        <ul class="song-list">
            <li class="song-item" onclick="showSongMessage('Perfect')">Perfect</li>
            <li class="song-item" onclick="showSongMessage('Until I Found You')">Until I Found You</li>
            <li class="song-item" onclick="showSongMessage('Eres')">Eres</li>
            <li class="song-item" onclick="showSongMessage('Amor Completo')">Amor Completo</li>
        </ul>
        <div id="song-message"></div>
        <button class="btn-back" onclick="clearSongMessage(); changeScreen('screen2')">Volver</button>
    </div>

    <!-- --- LÓGICA (JavaScript) --- -->
    <script>
        // Cambiar entre pantallas con transiciones suaves
        function changeScreen(screenId) {
            const activeScreen = document.querySelector('.screen.active');
            const nextScreen = document.getElementById(screenId);
            
            if (activeScreen) {
                activeScreen.style.opacity = '0';
                activeScreen.style.transform = 'scale(0.8)';
                setTimeout(() => {
                    activeScreen.classList.remove('active');
nextScreen.classList.add('active');

// Pequeño delay para que el navegador procese el cambio
// antes de animar
setTimeout(() => {
    nextScreen.style.opacity = '1';
    nextScreen.style.transform = 'scale(1)';
}, 50);

}, 400); // Duración de la transición de salida
}


// ===============================
// FLOATING HEARTS
// ===============================

function createHeart() {

    const heart = document.createElement('div');

    heart.classList.add('heart-particle');
    heart.innerHTML = '❤️';

    heart.style.left = Math.random() * 100 + 'vw';
    heart.style.animationDuration = (Math.random() * 3 + 4) + 's';
    heart.style.fontSize = (Math.random() * 15 + 15) + 'px';

    document.body.appendChild(heart);

    setTimeout(() => {
        heart.remove();
    }, 7000);

}

setInterval(createHeart, 400);


// ===============================
// NO BUTTON
// ===============================

function moveNoButton() {

    const btnNo = document.getElementById('btn-no');
    const container = document.getElementById('quiz-actions');

    const containerWidth = container.clientWidth;
    const containerHeight = container.clientHeight;

    const btnWidth = btnNo.offsetWidth;
    const btnHeight = btnNo.offsetHeight;

    const randomX = Math.random() * (containerWidth - btnWidth);
    const randomY = Math.random() * (containerHeight - btnHeight);

    btnNo.style.left = randomX + 'px';
    btnNo.style.top = randomY + 'px';

}


// ===============================
// YES BUTTON
// ===============================

function selectYes() {

    const title = document.getElementById('quiz-title');
    const actions = document.getElementById('quiz-actions');

    title.innerHTML = '❤️ ¡Yo también te quiero muchísimo! ❤️';

    actions.style.display = 'none';

}


// ===============================
// RESET QUIZ
// ===============================

function resetQuiz() {

    document.getElementById('quiz-title').innerHTML =
        '¿Me quieres? 🥺';

    const actions = document.getElementById('quiz-actions');

    actions.style.display = 'block';

    const btnNo = document.getElementById('btn-no');

    btnNo.style.left = '';
    btnNo.style.top = '';

}


// ===============================
// SONG MESSAGE
// ===============================

function showSongMessage(songName) {

    const msgDiv = document.getElementById('song-message');

    msgDiv.style.opacity = '0';

    setTimeout(() => {

        msgDiv.innerHTML = `"${songName}" me recuerda a ti ❤️`;

        msgDiv.style.transition = 'opacity 0.3s';
        msgDiv.style.opacity = '1';

    }, 150);

}

function clearSongMessage() {

    document.getElementById('song-message').innerHTML = '';

}
